import React from "react";

export const page = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            Contact page
        </div>
    );
};

export default page;
