import React from "react";

const page = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            Services page
        </div>
    );
};

export default page;
